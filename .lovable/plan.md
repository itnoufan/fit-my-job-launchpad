

## Plan: Make Course Titles More Eye-Catching

### Changes to `src/components/landing/CoursesSection.tsx`

**Enhanced title styling (line 87):**
- Increase font size to `text-2xl` for more visual weight
- Add a subtle text-shadow or use a richer gradient with wider color stops
- Wrap title in a container with a left accent border (3px gradient bar) for editorial flair
- Widen the underline accent from `w-8` to `w-12` and increase thickness to `h-1`

**Result:** Course names like "UAE HR Management" will pop with larger, bolder gradient text and a strong visual anchor via the accent bar.

### Files to Edit
- `src/components/landing/CoursesSection.tsx` — title size, accent border, wider underline

